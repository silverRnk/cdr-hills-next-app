import React from 'react'


const StudentsLayout = ({
    children,
  }: {
    children: React.ReactNode;
}) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default StudentsLayout